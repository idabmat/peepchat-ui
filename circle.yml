machine:
  node:
    version: 8.1.3
dependencies:
  pre:
    - node --version
    - npm --version
  override:
    - npm -g install npm@latest
    - npm install
test:
  override:
    - ember test
deployment:
  staging:
    branch: master
    heroku:
      appname: fathomless-cove-77494
